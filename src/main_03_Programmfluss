#include <iostream>

int main(){
  int array[5]={}
  for(index=0;index<5;index++){
    array[index]=3*index;
    if(array[index]>=6){
      std::cout<<"Array["<<index<<"] = "<< array[index]<<std::endl;
    }

  }



  return 0
}